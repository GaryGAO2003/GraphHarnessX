# GHX 过夜研究作战文档 · 08-23 → 08-24

用户令:①提高 GHX ship 率、把注意力从恒死题挪开,让环真正找到有问题的点;②精简 message、缩减预算;③全部已暴露缺陷+修复设计。"最后的机会,不许停。"

## 约束

- **不跑任何新 LLM 战役**(8-27 冻结纪律;live 跑候选写进报告由用户晨间裁决)。
- 只做盘上冻结档案的离线分析 + 机制设计 + 缺陷盘点。
- repo 只读;脚本与中间产物进 session scratchpad;过夜不 commit。
- 本文档为过夜期间唯一权威状态;各波裁决按序追加。

## 波次

**W1(并行,已发射)**
- A1 候选漏斗:M22/M24/M25/M26 各战役"Planner 瞄准→Evolver 产出→compose→门→ship→回滚"逐级存活计数,找最大流失点。
- A2 meta 角色 token 剖面:四角色逐轮成本/步数;Evolver 类 agentic 环是否每步重计费大载荷(单发消化器同款病);含 M14 步数减半 vs M26b"双环共有"对账。
- A3 solver 死段普查:字面重复空结果调用/循环窗/预算超限尾巴占 solver token 比例(T-17 池定容)。
- A4 缺陷总账:P 系/IV 系/各战役 bug 账全量盘点,状态裁决(open/fixed@commit/retracted),按影响面分类。
- A5 摇摆题逐题验证(前一轮已在飞):noop 边界翻转 vs ship 边界翻转。

**W2(视 W1 结果)**:锥选回放 gate 保真度@成本;Planner 瞄准桶 vs 历史转化率账;各设计细化。

**W3(主循环合成)**:三问各出"证据+机制设计+冻结前行动清单"→ 终版报告追加于本文档;memory 同步。

## 停止条件

三问各有三件套,或数据不可得被证明。达成前不停。

---

(以下为过夜追加区)

## W1·A5 裁决 — 摇摆题=概率,逐题坐实(08-23 夜)

五战役 291 个摇摆题、2653 个 noop 边界 + 3178 个 ship 边界的全量检验(方法:按各题自身 ship/noop 机会占比做曝光归一;复用 `flip_ledger.py` 官方分桶,与 `build_flip_ledger()` 四战役全对上):

- **汇总:noop 边界翻转率 21.0% vs ship 边界 20.2%,z=−0.78,不可区分**——ship 不比 noop 多翻。
- **逐题:83.2%(242/291)= 纯概率(PROB);仅 8.6%(25/291)= CONFIG-LINKED**(翻转集中在 ship 边界超出机会基率;全表 scratchpad/swingers_full.csv);8.2% 样本不足。
- **最干净的对照:e2_A(全程零 ship)**——51 个摇摆题照样翻了 259 次,ship 变量恒为零时约一半任务池仍在摇。
- noop 段的分数波动 ≤ 独立伯努利理论值(4 个无 carry 战役中 3 个),同配置段没有超额波动。
- **唯一例外:M25_103x16**(ship 22.5% vs noop 16.9%,z=+2.46)——但其最大单次跳变(R2)被当时三尺判在包络内;M24-R7、M26-R10 的 EXCESS 波对上了 25 题名单中的 5 题(triage 交叉验证)。
- **附带新缺陷**:`curves.json` 的 `evolve_status` 不可作 ship/noop 信号(M22 R1/R4 config 字节级同 R0/R3 却标 "ok")——只能用 `ship_outcomes.json` ∪ `journal.md` 的 action 字段。

**对三问的含义**:每轮"可见失败面"= 恒败(能力型,~7-14 题)+ 摇摆(83% 纯噪声)——**环盯着的目标池里,能被编辑真实回报的不足一成**;分数对门的每轮反馈≈纯噪声,门读总分等于掷硬币,必须改读机制计数器。真靶点=机制阻塞类失败(budget/motif)+ 25 个 config-linked 摇摆题(名单已存)。

## W1·A1 裁决 — 候选漏斗:环不缺候选,缺的是不被否决的通道(08-23 夜)

16 个战役全量:**142 生成 → 139 过 compose → 65 ship → 58 幸存**。三大流失点(按大小):

1. **Critic no_op 口头否决 40/139(28.8%),最大单级流失**——no_op 时 ship_ranking 为空,**零个机械门被运行**,否决靠不可证伪的散文;其中 8 个因 `critic_failed` 提前 return(orchestrator.py:613-617)**零账本记录彻底蒸发**。修:账本写入提前到 early-return 之前;no_op 倾向下仍强制头名候选过机械门,让"不 ship"也是门确认的。
2. **机械门 32/139(23.0%)**:structure/IV-11=23(候选 bucket 不理 Planner 旗标),graph_existence=7(编辑没被执行踩到的节点;M26b 里占该战役门杀 7/11)。修:把这两个门做成 Evolver 生成期自检(propose.py 已有同款 validate 模式),把 stage-4 报废变成生成时自纠。
3. **runner 级分数回滚 7/65(10.8%)**:门槛写死 "R0 方差±4",实测同配置 Δ 分布含 −11 离群,~14% 噪声误杀;M25-R2 杀掉 −90% 机制修复即此门。修(事故文档自己已提):回滚触发改挂 Critic 的 counterfactual 合格硬回归计数,总分-only 触发降级为"标记再观察一轮"。

**Evolver 干涸不是主因**:全 110 轮里确证零候选轮仅 2。记忆中"M25 两次 400 步烧穿"在本仓库快照中未证实(无零候选轮、无 400 步配置)——挂起待 A2 的 trace 级核对。另:e2_A 是另一套变体池架构,R0-R10 十一轮压根没尝试过候选,20 个候选 70% 死在 PIPELINE_CRITIC_INITIAL,零采纳。

## W1·A3 裁决 — T-17 判死:solver 侧可证死段池 <0.3%(08-23 夜)

M25(1435 轨迹)+ M26b(700 轨迹)全量普查,逐步 token 与 TaskEnd 总账 0 误差对账:

- **安全池(空结果字面重复 + budget_exceeded 尾巴)= 0.05% / 0.24% 总 token,远低于 10% 生死线 → T-17 按原 scope 判死。**
- 全部字面重复也只有 3.2%/2.6%,其中约半是 Browser get_text 零参调用的**测量假象**(跟在不同 navigate 后,读的是新页面);真重复 ≈1.6%。
- 循环窗(同工具名≥3 连发)占 47% token 但 96.6% 是正常检索行为——工具名重复不是浪费信号,再次印证 M13 教训。
- **budget_exceeded 轨迹是"死在探索中途"不是"死在空转"**:119 条里仅 3 条有任何尾巴浪费 → "把饿死题的浪费挪给别人"的前提不成立。
- **真正的成本结构是上下文复计费**:step1→20 输入 token 增长 9.6×/5.8×,旧工具结果逐步重复进账——solver 侧省钱的活靶子是**消息装配/压缩策略**,不是删死段。→ W2·B2 定量。

## W1·A2 裁决 — meta 成本剖面:Evolver 72.6%,且全网零缓存命中(08-23 夜)

从原始 trace 独立重算(与 M25 公布值 $438/中位 205 步分毫对上;两个 schema 陷阱已修:分段 cumulative 取 max 不能求和,逐调用 usage 求和安全):

- **角色分布(M25,13 轮)**:Evolver $438.10(72.6%)/ Critic $96.69(16.0%)/ Planner $68.21(11.3%)。Digester 单发化后无 session 目录=结构性省掉,交叉印证。
- **Evolver 不是消化器形状**:分段锯齿(Compaction 300k 阈值/0.9 驱逐/留 4),压缩后地板 10-28K 每步重付 ≈31% 输入质量,69% 是真增量——**单发化不适用,杠杆是"别重复付费"**(证据外置按需取回 ≈$10/轮)。
- **全网零缓存命中**:M25 3,687 + M26b 2,760 + M26 292 次 meta 调用,`cache_read/write_tokens` **无一非零**。DeepSeek 缓存读 ≈50× 便宜(provider 代码自注),即 230M+ meta 输入 token 一分钱折扣都没拿到。根因未判(provider 探测管道在,litellm_provider.py:82-98;疑网关不把同会话重复调用钉到同后端——与模型路由漂移旧账吻合)——**这是唯一零行为影响的纯计费杠杆,晨间第一优先:同前缀双调用探针**。
- **"400 步烧穿"对账闭合**(A1 的悬案):M25-R8/R11 = 各 10 个 compaction 分段、$65.78/$59.75——是"高步数轮"不是"零候选轮",A1 结论(环不缺候选)不受影响。
- **修剪排序**:①修缓存(零风险,上限最大)→ ②Evolver 证据外置 ≈$10/轮 → ③Critic 单发 ≈$6/轮(高风险:Critic 有否决权,需先做 A/B)→ ④Evolver 烧穿轮步帽 ≈$61/战役 → ⑤Planner 单发 ≈$4.3/轮。
- **M14 对账终裁**:窄主张**幸存**——步数减半从原始 trace 精确复算(61/62→30/25,196/261→151/161),且工具调用构成检验(L0 Planner 51 Bash/30 Glob 裸勘 vs L2 大幅减少)排除了 P-15 两个小字段 bug 的干扰;宽外推**被驳**——M26b 账本只给它 $3/$58,且 M22-L0 vs M26b 是 L0-vs-L5 跨期混杂,非同机制复测;L5 下是否仍复现,**数据不足**(M14 后无干净对照对)。

## W1·A4 裁决 — 缺陷总账:19 开放 / 34 已修 / 2 撤回 / 4 判非缺陷(08-23 夜)

全量清点(vendored-deviations P-1..P-26 + v6 build log + M22/M24/M25 文档 + git log + 直接读码),无一条需要 VERIFY-FAILED。**状态修正**:P-21(回滚对 Planner 不可见)已修@`29c1860`,写→读→模板全链路码上确认,全账本置信度最高的修复;content-ref 压平修复在 `03d4b42`;"predicted_tasks ID 零命中"= P-15(b) 截断 + P-20 幻觉尾巴两条合治(`e7e98d8`/`d1c84d0`)。

### 开放缺陷(19 条,按影响面)

**治理(9)**
| 缺陷 | 一句话 | 状态/修法 |
|---|---|---|
| **runner 分数回滚门不读三尺** | `run_meta_aegis.py:1272-1278` 裸 Δrate/Δcount 触发,grep 全文件零处引用 regression_triage;实测倒推 ~14% 噪声误杀;M25-R2 事故即它 | OPEN;修=把 `compute_triage()` 的 stat_verdict/counterfactual 接进回滚分支——**代码已存在,只差接线** |
| loop_health 零生产调用点 | 6 项健康检查只有 CLI 手动 | OPEN(M24-T24);修=接进轮循环自动跑 |
| T-25 灭因账读侧未成简报 | 死因不进 Planner/Digester 上下文,Evolver 重复提案已死候选 | OPEN(backlog) |
| IV-4 broken_verdict 计数零读者 | 写了没人看 | OPEN |
| verdict 契约双写漂移 | 软失败不回传 | OPEN(CH4 方向) |
| BaseTask 无 task_type | 预算表无键可挂 | OPEN(小) |
| **活体 U 零轮内消费者** | 图证据全部轮间粒度 | OPEN/结构性;要么接 runloop hook,要么论文明写 by-design |
| P-6 early-stop patience=2 | 格式彩票期可截臂 | OPEN(补丁已拟,押后) |
| read_rejected_candidates 孤儿函数 | 死代码 | OPEN(卫生) |

**预算(5)**:Evolver 烧穿 PARTIAL(安全网@`44520a3` 但 M25 仍 2/13 轮命中;**P-1..P-5 提示词补丁已拟未 commit**)· Stage-P 无上限(P-10b 未建,理论敞口 ~$10,300)· noop 轮 config-hash 漂移(base_dir 漏进哈希,两处同族)· noop_streak 基线错(回滚后比较基准不对,L0 十六轮 R13 假收敛提前 3 轮)· tau2 网关挂起无硬帽。

**ship 率(1)**:YAML 候选死亡面未修源头(15 轮 1.26B token 零采纳的主凶),现行出路=扩大 L5 机器可查表面。

**测量(2)**:P-3 judge 锚点契约(裁定不上)· 回归账 off-by-one 官方臂故意保留(引用其 regressions.md 须标注)。

**床效度(2)**:L2 空锥(160/160 退化,结构性,战役中不修)· GAIA 软泄漏旗仍标 27-42% 通过题。

### 已修(34 条,摘要)
P-1/2/4/5/7/9/11/12/15/16/18/19/20/21/22/23/24/25/26、IV-9、slug 双形态、五层烟测链、第六门 0/6→4/4、tau2 步顶自测@`2b87c26`、content-ref@`03d4b42`、outcome-fidelity ×20@`a041a7d`、judge 硬化、回归账 GHX 侧@`df17af9`、硬泄漏黑名单(残余 2.4%)等。撤回:P-17、格式彩票因果;判非缺陷:小床阈值失活、403 对称、SERPER 不接线(官方保真)。

### 两张最高杠杆榜
**提 ship 率**:①回滚门接三尺(接线级)②扩 L5 绕 YAML 死亡面 ③落 P-1..P-5 治烧穿 ④修 noop_streak 基线 ⑤建 T-25 死因简报。
**省预算**:①落 P-1..P-5(烧穿 $27-100/轮)②建 Stage-P 上限 ③回滚门接三尺(每次误杀=整轮算力作废)④tau2 硬超时 ⑤修 config-hash 漂移。

## W2·B3 裁决 — 瞄准账:88% 的注意力打在不会回报的靶上,且瞄准不改变命运(08-24 凌晨)

65 个候选(M24/M25/M26)、294 个"候选→点名任务"实例,fate 桶按 ship 前一轮的即时状态计(避免回溯桶的同义反复):

- **注意力分布(pooled)**:NEVER 31.0% + PROB 摇摆 56.8% = **87.8% 的点名落在恒败题和概率摇摆题上**;CONFIG-LINKED 仅 7.8%。NEVER 题只占任务全集 13-14%,却吸走 24-38% 的点名——不成比例的凝视,定量坐实。每战役约 60% 任务从未被任何候选点过名。
- **转化率 vs 底率(致命表)**:PROB 摇摆:被瞄准 50.0% vs 没被瞄准 51.5%(p=0.865)——**主力靶上,瞄准与不瞄准无差**,环给自己记的功约一半本来也会发生。NEVER-so-far:24.4% vs 16.9%(p=0.128,方向正但不显著,M26 反向)。CONFIG-LINKED:85.7% vs 65.5%(n=7,太小)。**没有任何桶的瞄准增益达到显著。**
- **hit_rate 是又一个未校底率的假信号**:Critic 按面值记命中,而 PROB 靶自带 ~50% 底率——与 F9 的 lift 口径(已校底率)相比,原生 hit_rate 系统性虚高。唯一机制类候选(M25 C-R13-01)predicted_tasks 留空 → hit_rate 记 0/0 不可记功;其散文引的一次翻转恰是 CONFIG-LINKED 题的假记功形状。
- **Planner 结构性失明(码上钉死)**:`PlannerInputs`(planner.py:30-46)无任何桶/翻转/转化先验;模板只让它"自己去 eyeball task_history.jsonl";guidance 的 rebind 列表(guidance.py:328-330,372-374)只重绑 Evolver/Critic,**从不重绑 Planner**——结构性排除,非旗标遗漏;flip_ledger.md 仅 M26 存在(13/14 轮),且只有原始计数,无 PROB/CONFIG-LINKED 裁决、无转化先验。**三个战役里,没有任何角色在任何一轮拿到过转化先验。**
- **修复六接缝**(全部复用现有布线,B3 报告有行号):①flip-ledger 进 PlannerInputs+allowed_read_files ②guidance rebind 列表加 Planner ③candidate_scope 加 fate 桶查验(与 cone-reach 并排)④第七道门:多数-NEVER 预测集须给 override 理由 ⑤Critic 锥定价扩展:NEVER 桶命中按经验底率计价 ⑥reputation 加 fate 桶维度(Planner 已在读 reputation,免费搭车)。

**升华到 V3 第 6 幕**:就算六接缝全落地,也只是把瞄准从"不可能回报的靶"挪到"可回报的靶"——而全部四场战役没有一个候选瞄准过**方差本身**(47 题可达不稳,F16)。瞄准修复是近期工程;瞄准方差是下一个研究问题。

## W2·B2 裁决 — 消息面保留=真正的预算主杠杆:26-33% 总成本,今日捕获率 0%(08-24 凌晨)

2,318 条轨迹逐步重建消息面(校准 R²=0.958,字符按实际计费 input_tokens 比例分摊,总量精确对账):

- **陈旧工具结果占输入质量:>3 步龄 41.4%(M25)/ 33.3%(M26b)**;失败轨迹的陈旧占比是通过轨迹的 1.5-1.7×——失败跑是"淹死在旧上下文里",不只是"跑得更久"。
- **策略模拟**:(b) 3 步龄外结果截为 200 字符桩 + 错误结果与最近 3 条豁免 + 被助手引用者保留 → **省 30-37% 输入质量 ≈ 26-33% 战役总美元**(输入:输出 ≈23:1,输入占 ~88% 成本;两战役合计 ≈$128)。滑窗 keep-20 只省 16-22% 且会丢错误结果与整段推理,劣于 (b)。风险面:严格口径下 13-14% 的调用后来在新工具调用里复用过被桩内容——(b) 的引用豁免堵住助手引用通道,工具调用复用通道仍开,需在实现时补"桩可展开"(桩里留取回指针)。
- **今日捕获率 = 0%,三重实锤**:TokenBudgetProcessor 反应式触发线 85 万 token(deepseek-v4-flash 1M 窗),全部 2,318 条轨迹单步峰值 10.4 万——**从未触发过一次**;76 份 config.yaml 零 memory/compaction 处理器;solver 侧 cache_read 同样全零(与 meta 侧 A2 发现同族)。
- **归因诚实**:此杠杆 **HX-generic,不归图**——M25/M26 两臂上下文处理布线相同(RuntimePolicy 不碰保留)。唯一的图接口:keep-if-referenced 的文本重叠是"可达性"的粗代理,而可达性追踪正是活体 U 记录器结构上该干的事——**图内生的保留策略是合理后续,但现在不存在,只能按能力写**。
- **零风险附赠**:PNG 被当文本 Read 的 ~90KB 乱码横贯两战役多数轮——检测非文本输出拒绝内联,无条件安全,独立可修。

## W2·B1 裁决 — 锥选回归集判死:输给翻转史簿记,且会放过唯一真回滚(08-24 凌晨)

复用仓库自己的锥/三尺原语回放(方法学发现:`R{X}/graph_evidence/cone_sigs.json` 反映的是 **X-1 轮**的迹,调用点+数值双证;triage 复算与盘上 regressions.md 逐字节对上;M26 noop-audit 的 75% 携带任务用"回看至上一 ship 轮"恢复 100% 覆盖):

- **18 个可算 ship 轮、167 个三尺合格硬回归**。锥选:召回 31.7% @ 成本 30.3%——与同成本盲随机(30.1%)不可区分,**被"选历史翻得最多的题"这个平凡簿记打败**(33.5%;在锥有信息的 7 轮子集里 86.2% vs 81.5%)。
- **安全面致命伤**:全语料唯一一次真实回滚(M25-R2),两个 ship 都锥不可见(一个无节点、一个节点当轮新生)——锥选门会**无声放行**它。M26-R10 的 EXCESS 告警同理漏报(12 个硬回归仅 1 个 in-cone)。
- **结构根因(不是调参问题)**:61% 的硬回归来自 pre-ship 足迹为零的新节点——新机制按构造不可能出现在旧锥里(`candidate_scope.py:8-19` 自己写着)。**图的旧迹可以否决新东西(F11 重放门 7 次结构性拒绝用的就是这个),但测不了新东西的爆炸半径**——同一个事实,门用对了方向,选择器用反了方向。
- 判决:**锥选回归集不构成图可归因的降本主张**。原文:"a coin flip weighted slightly worse than 'which tasks have been noisy lately', dressed in causal vocabulary."

---

# W3 · 终合成(08-24 凌晨)

## 三问终答

**① ship 率 + 恒死题注意力**:环不缺候选(142 个,真干涸轮仅 2),死于三级否决——no_op 口头否决 28.8%(8 个零记录蒸发)、机械门 23%(生成期可自纠却在 stage-4 报废)、回滚门 ~14% 噪声误杀(runner 门 grep 零处引用三尺,纯接线缺失)。瞄准面:87.8% 点名落在恒败(31%)+概率摇摆(57%)上;主力靶 PROB 的瞄准转化 50.0% vs 底率 51.5%(p=0.865)——**瞄准不改变命运**;无任何角色任何轮拿到过转化先验(Planner 被 guidance rebind 结构性排除)。修复包(全接线级):回滚门接三尺 / no_op 也过机械门+记账前移 / 门前置为生成自检 / Planner 六接缝喂桶+先验 / P-1..P-5 烧穿补丁 / noop_streak 基线。真靶点=CONFIG-LINKED 25 题+机制类;最深的一层:四战役零候选瞄准过方差本身(F16)。

**② message 精简/预算**:三层定容——计费层:**meta+solver 全网零缓存命中**(9,200+ 调用无一非零;DeepSeek 缓存读 ~50× 便宜),零行为风险,上限最大;消息层:**保留策略 (b) = 26-33% 战役总美元**,今日捕获率 0%(TokenBudgetProcessor 从未触发过一次);动作层:死段 <0.3% 判死。meta 侧:Evolver 72.6% 集中、地板复付 ≈31%(证据外置 ≈$10/轮)、烧穿轮步帽 ≈$61/战役;Critic/Planner 单发需先 A/B。附赠零风险:非文本输出拒内联。

**③ 缺陷全账**:19 开放 / 34 已修 / 2 撤回 / 4 判非缺陷,两张杠杆榜见 A4 节。

## 图的成本轴终裁(闭合 08-23 预算轴评估)

两个"由构造归因给图"的候选形状**双双阵亡**:T-17 池 <0.3%(A3)、锥选输给簿记且漏真回滚(B1)。成本正项(缓存/保留/单发/烧穿治理)全部 HX-generic——与 F13"图层净贵"、runbook 原判一致。**但这两条离线裁决本身是 V3 第 4 幕"图的限度"的新证据**:图的 pre-ship 迹对新机制是结构性盲的——这同时解释了重放门为何有效(用盲区做否决)与锥选为何失效(用盲区做预测)。方法(回放+对照选择器)与全部脚本可固化为 audit_*.py 进 F 台账。

## 与 V3 台账的对接(候选新 F 行,脚本已在 `experiments/analysis/overnight_0823/`)

- 摇摆逐题归因:83.2% PROB / 8.6% CONFIG-LINKED,noop 21.0% vs ship 20.2%(z=−0.78)——F15 的逐题延伸,独立互证 20.5%。
- 瞄准无增益:PROB 靶 50.0% vs 51.5%(p=0.865);NEVER 占 13-14% 任务吸 24-38% 注意力——第 6 幕定量。
- 漏斗:142→65→58,三级否决占比——F10 的"为什么"。
- 零缓存命中:9,200+ 调用全零——新 A 类事实。
- 消息面陈旧占比 41.4%/33.3%,策略 (b) 26-33%——HX 工程贡献。
- 锥选回放:31.7% ≈ 随机 30.1% < 簿记 33.5%;61% 硬回归零 pre-ship 足迹——图限度新证据。

## W4·C1 裁决 — 方差来源普查:GO(08-24 日间)

63 个不稳题(≥3过≥3败,M25 38 + M26b 26 - 1 数据质量排除)逐题成败轨迹对齐:

- **LOCALIZED-FIXABLE 28/63(44%)**,两战役独立各自过线(18/38、10/26);**潜在增益 Σ(0.9−通过率)=8.85 题当量,过 7 题可见线**;放宽到 ≥2/≥2 → 40 题/13.04,同向更大。
- **DIFFUSE-SAMPLING 仅 5/63(8%)**——"全是采样噪声"的担忧死了;AMBIG 30/63(48%),按保守纪律不记增益。
- **主导可修形状 = "long-then-wrong"**(22/28 题以 total_steps 为最强分离特征):失败轨迹比同题通过轨迹长得多,却以 done+错答自行收尾(可修池失败解剖:62% 跑完答错 vs 38% 预算死);其中 11 题失败轨迹的空结果率比通过高 ≥20pp——**撞空不改策略,磨到没劲然后编造**。
- **环境借口双杀**:分叉点前同签名网络调用成败结果不同 = 0/63(严格测试干净阴性);403 类命中在同题成败轨迹间近似均布,且混着 harness 自己的确定性 query 黑名单——不是随机网络,是自家旋钮。
- **方法学陷阱(自曝)**:严格文本 LCP 61/63 题中位=0——全被 WebSearch 措辞重写噪声淹没,险些误判"全弥散";粗签名(WebSearch 折叠为工具名、URL/命令保留目标)才是可用信号。测量教训归入 §9 家族。
- **三个稳定器设计**:①撞空升级门(连续 k 次空/无用结果 → 强制换策略,loop_detection 的姊妹件但键在结果质量)②**答前引用校验门**(终答必须引用轨迹内非空工具结果的具体事实,否则强制补一步检索)③软预算答案检查点(70-80% 预算处强制 best-guess 提交,CostGuard 扩展)。
- **两个论文级对称性**:(i)稳定器② 强制的正是 V3 主张里"环测不了的量"(输出是否实际被使用,F1-F8)——**测量失败的量,可以在答案关口被强制**:审计章的反派变成机制章的主角;(ii)稳定器③ ≈ M25 被误杀的 C-R2-02(budget_no_commit −90% 那个)复活——机制价值已有活体证据,死因是门,门已在修复包里。
- 读数设计:不读总分,**28 题靶集配对 McNemar**(F21 模块现成;8修1破即 p≈0.039)。
- 悬项:swingers_full.csv 的 CONFIG-LINKED 列与 28 题交叉(区分"config 变化型一致分叉");853c8244 多 run_id 目录人工解结(两战役同现的 crash-retry 症状本身值得命名);AMBIG 30 题二次细分押后。
- 脚本+全表:experiments/analysis/overnight_0823/(fork_lib、step1-4、unstable_task_fork_analysis.csv)。

## 晨间裁决单

1. **缓存探针**(同前缀双调用,几分钱):确认网关是否可命中缓存;命中→修路由=最大零风险省钱;不命中→换支持缓存的接入方式或写死此损耗。
2. **修复包落地与否**:六项接线级修复;V3 框架下按"能力交付+效果未测"写亦成立,是否动用冻结例外跑 30×5 级验证由你裁。
3. **保留策略 (b)** 实现为 processor(小);论文按 HX 工程贡献写。
4. **脚本提升**:overnight_0823/ 内脚本若进 F 台账需按 audit_*.py 惯例固化+复算日期。
5. **双阴性入章**:T-17 与锥选两条裁决建议进 CH6/CH7(图的限度),与 F11 的盲区对称性是现成的一段。
